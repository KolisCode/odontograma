export const environment = {
  production: true,
  // Dejar vacío si Express sirve el frontend desde el mismo servidor (mismo origen).
  // Cambiar a 'http://IP_DEL_SERVIDOR:3000' si corren en puertos distintos.
  apiUrl: '',
};
